# 推广系统要求内容和截止时间功能增强总结

## 新增功能
在Create Promotion Task页面增加了两个新参数：
1. **要求内容 (Requirements)** - 用户可以自定义推广任务的具体要求
2. **截止时间 (Deadline)** - 用户可以手动设置推广任务的截止日期

## 修改内容

### 前端修改

#### Advertiser.tsx
- 添加了 `requirements` 和 `deadline` 状态变量
- 在表单中添加了要求内容文本区域字段（支持多行输入）
- 在表单中添加了截止时间日期选择字段（限制最小日期为今天）
- 更新了表单提交逻辑：
  - 使用用户输入的要求内容（按行分割为数组）
  - 使用用户输入的截止时间，如果未提供则使用默认7天后
  - 提供了默认要求内容作为后备

#### PromotionTasks.tsx
- 更新了任务要求部分的标题为"任务要求:"
- 保持现有的要求内容和截止时间显示逻辑

### 后端修改

#### controller/promotion_controller.rs
- 后端数据结构已经支持requirements和deadline字段
- 无需额外修改，现有的CreatePromotionTaskRequest结构体已包含这些字段

## 验证结果

### API测试
✅ **GET /promotion/tasks** - 返回包含要求内容和截止时间的任务列表
✅ **POST /promotion/tasks** - 成功创建包含自定义要求内容和截止时间的推广任务

### 功能验证
1. **创建推广任务** - 用户现在可以输入自定义要求内容和截止时间
2. **查看任务列表** - 任务列表正确显示要求内容和截止时间
3. **数据持久化** - 所有字段都正确保存到数据库

## 用户体验改进
1. **灵活的要求设置** - 用户可以自定义推广任务的具体要求，每行一个要求
2. **自定义截止时间** - 用户可以手动设置截止日期，而不是使用固定的7天后
3. **更好的任务管理** - 提供更精确的任务参数控制

## 字段说明

### 要求内容 (Requirements)
- 类型：多行文本输入
- 格式：每行一个要求，支持中英文
- 默认值：如果用户未输入，使用默认要求
- 处理：前端按行分割为字符串数组发送到后端

### 截止时间 (Deadline)
- 类型：日期选择器
- 限制：最小日期为今天
- 默认值：如果用户未选择，使用7天后
- 格式：YYYY-MM-DD

## 测试步骤
1. 访问 http://localhost:5174/
2. 导航到 Advertiser 页面
3. 填写任务名称、推文URL、奖励USDC金额
4. **输入要求内容**（每行一个要求）
5. **选择截止时间**
6. 创建推广任务
7. 导航到 PromotionTasks 页面查看任务列表，确认要求内容和截止时间正确显示
